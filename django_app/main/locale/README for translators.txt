These are the formats used for translation:

lbl_* = Labels
btn_* = Buttons
txt_* = Text inputs
tt_* = Tooltips (the message that appears when you pass the cursor over something such as links, buttons, etc)

